class Solution:
	# @param A : integer
	# @param B : integer
	# @return an integer
	def gcd(self, A, B):
        divisor = 0
		for i in range(1, max(A, B)+1):
			if A%i == 0 and B%i == 0:
				divisor = i
		return divisor
